#!/usr/bin/env node

const program = require('commander');

program
  .version('0.1.0')
	.command('mongo', 'Commands related to mongodb')
	.command('run', 'Get timeline from tweet')
  .command('utils', 'Differents utilities')
  .command('test', 'Testing')
  .parse(process.argv);
