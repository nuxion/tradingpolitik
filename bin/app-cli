#!/usr/bin/env node

const program = require('commander');

program
  .version('0.1.0')
	.command('loaddb [file]', 'Load to mongodb')
	.command('test [msg]', 'Test command')
  .command('utils', 'Differents utilities')
  .parse(process.argv);
